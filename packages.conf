# Arquivo de configuração de pacotes para arch-post-install
# Descomente os pacotes que deseja instalar, comente os que não quer

# ========== DESENVOLVIMENTO ==========
git
base-devel

# ========== COMPRESSÃO ==========
ark
unrar

# ========== TERMINAL & SHELL ==========
kitty
fish

# ========== FONTES NERD FONTS ==========
# 'nerd-fonts' é um GRUPO que instala TODAS as ~69 fontes de uma vez
nerd-fonts
# Se preferir apenas fontes específicas, comente a linha acima
# e descomente somente as que quiser abaixo:
# ttf-firacode-nerd
# ttf-jetbrains-mono-nerd
# ttf-hack-nerd
# ttf-noto-nerd
# noto-fonts-emoji  # (emojis coloridos, não é nerd font mas útil)

# ========== RUNTIME & LINGUAGENS ==========
nodejs
npm
jdk-openjdk
dotnet-sdk
dotnet-runtime
python

# ========== NAVEGADORES ==========
# brave está no AUR como 'brave-bin' (veja a seção AUR abaixo)
# firefox
# chromium

# ========== COMUNICAÇÃO ==========
telegram-desktop
discord
thunderbird
# signal-desktop

# ========== MULTIMÍDIA & DOWNLOAD ==========
qbittorrent
spotify-launcher
# mpv
# ffmpeg

# ========== GAMING ==========
steam
# lutris
# wine

# ========== UTILITÁRIOS & SISTEMA ==========
efibootmgr
okular
kate
gwenview
kcalc

# ========== ESSENCIAIS DO SISTEMA ==========
# (não vêm no archinstall mínimo — recomendados p/ funcionamento completo)
sof-firmware              # firmware de áudio (notebooks modernos)
power-profiles-daemon     # gerenciamento de energia (KDE PowerDevil)
networkmanager            # gerência de rede/Wi-Fi
bluez                     # stack Bluetooth
bluez-utils
xdg-desktop-portal-kde    # integração de apps Flatpak no KDE
p7zip                     # backend do Ark (.7z)
unzip                     # backend do Ark (.zip)
zip
firewalld                 # firewall
reflector                 # atualiza mirrors mais rápidos
pacman-contrib            # paccache, checkupdates
openssh                   # cliente/servidor SSH
man-db                    # páginas de manual
man-pages
wget

# ========== IMPRESSÃO ==========
cups
cups-pdf
print-manager
system-config-printer

# ========== FONTES ADICIONAIS ==========
noto-fonts-cjk            # caracteres asiáticos
ttf-liberation            # substitutos de Arial/Times
ttf-dejavu

# NOTA: microcode da CPU (intel-ucode/amd-ucode) e drivers de vídeo
# (mesa/vulkan) NÃO estão aqui porque dependem do hardware.
# Use o install.sh (detecta e instala automaticamente) para isso.

# ========== CONTAINER & VIRTUALIZAÇÃO ==========
podman-desktop
docker
docker-compose

# ========== BANCO DE DADOS ==========
dbeaver
# postgresql
# mysql

# ========== FLATPAK - Aplicações Flatpak ==========
# Adicione apps no formato: org.empresa.NomeDaApp
org.videolan.VLC
com.termius.Termius
com.heroicgameslauncher.hgl   # Heroic Games (Epic/GOG/Amazon)

# ========== AUR - Pacotes do AUR (via Paru) ==========
# Adicione pacotes do AUR aqui
brave-bin
onlyoffice-bin
visual-studio-code-bin

# ========== NPM - Pacotes globais do npm ==========
# Instalados via 'npm install -g' (requer nodejs + npm)
@angular/cli
# typescript
# nodemon
# pnpm
# yarn
